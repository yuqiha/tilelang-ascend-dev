# 代码格式检查报告

**检查时间**: 2026-03-25 14:07:26
**检查范围**: 全部文件

## 检查摘要

| 语言 | 文件数 | Lint 问题数 | 格式问题数 |
|------|--------|-------------|------------|
| Python | 276 | 3649 | 218 |
| C++ | 131 | 0 | 27 |

**总计**: 407 个文件，3649 个 Lint 问题，245 个格式问题

---

## Python 问题统计

### Lint 问题按代码分类 (Top 20)

| 代码 | 数量 | 描述 |
|------|------|------|
| W291 | 大量 | Trailing whitespace |
| W293 | 大量 | Blank line contains whitespace |
| W292 | 大量 | No newline at end of file |
| F401 | 大量 | Unused import |
| FA102 | 多个 | Missing `from __future__ import annotations` |
| F841 | 多个 | Local variable assigned but never used |
| F541 | 多个 | f-string without any placeholders |
| SIM105 | 多个 | Use contextlib.suppress instead of try-except-pass |
| UP009 | 多个 | UTF-8 encoding declaration is unnecessary |
| UP031 | 多个 | Use format specifiers instead of percent format |

### 格式问题文件 (218 个文件)

主要分布在:
- `.agents/skills/` 目录下的脚本文件
- `examples/` 目录下的示例文件
- `tilelang/` 核心代码
- `testing/` 测试文件

---

## C++ 文件问题详情 (27 个文件需要格式化)

| 文件路径 |
|----------|
| src/tl_templates/cpp/half.hpp |
| src/tl_templates/ascend/printf.h |
| src/tl_templates/ascend/common.h |
| src/op/builtin.cc |
| src/op/parallel.h |
| src/target/codegen_cuda.cc |
| src/target/cuda.h |
| src/target/codegen_ascend.cc |
| src/target/codegen_ascend.h |
| src/target/rt_mod_ascend.cc |
| src/target/rt_mod_ascend_pto.cc |
| src/transform/cluster_planning.cc |
| src/transform/ascend_pto_save_buffer_shape.cc |
| src/transform/common/operation_config.h |
| src/transform/common/loop_fusion_utils.h |
| src/transform/pipeline_planning.cc |
| src/transform/cross_core_pipeline.cc |
| src/transform/ascend_lower_parallel_to_vector.cc |
| src/transform/ascend_host.cc |
| src/transform/ascend_memory_planning.cc |
| src/transform/merge_shared_memory_allocations.cc |
| src/transform/ascend_sync_insert.cc |
| src/transform/inject_pipeline.cc |
| testing/testc.cpp |
| examples/gemm_aot/example_gemm.cpp |
| examples/torch_tl_ascend/demo_libtorch/flash_attention.cpp |
| examples/torch_tl_ascend/src/_inner.cpp |

---

## 下一步操作

### Python 修复

```bash
# 自动修复 lint 问题
ruff check --fix .

# 格式化代码
ruff format .
```

### C++ 修复

```bash
# 格式化所有 C++ 文件
find . -type f \( -name "*.c" -o -name "*.cc" -o -name "*.cpp" -o -name "*.cxx" -o -name "*.h" -o -name "*.hpp" -o -name "*.hh" -o -name "*.icc" \) \
    ! -path "./.git/*" ! -path "./build/*" ! -path "./third_party/*" \
    -exec clang-format -i --style=file {} \;
```

### 验证修复

```bash
# 验证 Python
ruff check .
ruff format --check .

# 验证 C++
clang-format --dry-run --Werror <files>
```